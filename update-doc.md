All of the features described in md files in the feature folder have been implemented. Now I want to extract all important information from those files and the code, in order to have the most important patterns written down in what I call "production md files", meaning the project documentation files (AGENTS.md, CLAUDE.md (symlinks to AGENTS.md, README.md, INSTALL.md, etc.). I need you to identify all the project documentation files ("production md files"), make sure that they are up to date with current code patterns, and especially, that important patterns from the implementation md files in the feature folder are documented. The most important goal is too keep coding agents productivity, code quality and efficiency high. However, of course the production md files shouldn’t be too verbose either. Make the analysis and give me an high-level overview of the changes you suggest to make to the production md files, so we can review them together before you write them. Also, if not present, make an addition in the main AGENTS.md at root level to tell the agent to proactively suggest updating documentation whenever a non-trivial or important pattern is implemented or changed in the code. This way we can keep the documentation up to date more easily in the future.